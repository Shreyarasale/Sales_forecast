# Forecast Engine

Lightweight Flask service for forecast generation.

## Run

1. Create venv and install:
   pip install -r requirements.txt
2. Start service:
   python app.py

Service runs on port 5001.
